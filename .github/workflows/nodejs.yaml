name: My CI-CD 
on:
  push: 
jobs:
  build: 
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v3
      - run: curl -L https://nixos.org/nix/install | sh
      - run: nix develop --file tests.flake --command hello
