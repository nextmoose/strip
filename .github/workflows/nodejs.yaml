name: My CI-CD 
on:
  push: 
jobs:
  build: 
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v3
      - run: curl -L https://nixos.org/nix/install | sh
      - run: pwd
      - run: cd tests
      - run: pwd
      - run: nix develop --command hello
